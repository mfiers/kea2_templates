#p input
#p output default={{input}}.gz

### prolog

module load pigz || true

### main

## Output: {o} {{input}}.gz
## Input: {i} {{ input }}

if (which pigz);
then
    pigz -9 {{ input }}
else
    gzip -9  {{ input }}
fi